{
  "name": "unipass-wallet",
  "description": "Example of using ts-node in a TS monorepo",
  "version": "0.0.6",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "test": "jest --testTimeout 60000",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "ethers": "^5.6.9",
    "unipass-wallet-dkim-base": "^0.0.6"
  },
  "devDependencies": {
    "@jest/types": "~28.1.0",
    "@types/jest": "~27.5.0",
    "@types/node": "^18.0.5",
    "@types/node-rsa": "^1.1.1",
    "@types/nodemailer": "^6.4.4",
    "dotenv": "^16.0.1",
    "hardhat": "^2.10.1",
    "jest": "~28.1.0",
    "nodemailer": "^6.7.2",
    "rimraf": "~3.0.2",
    "ts-jest": "~28.0.0",
    "typescript": "~4.7.0",
    "unipass-wallet-dkim": "^0.0.6"
  }
}
