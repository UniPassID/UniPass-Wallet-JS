{
  "name": "unipass-wallet",
  "description": "Example of using ts-node in a TS monorepo",
  "private": true,
  "version": "0.0.1",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "ethers": "^5.6.9",
    "unipass-wallet-dkim": "^0.0.1"
  },
  "devDependencies": {
    "@types/node": "^18.0.5",
    "rimraf": "~3.0.2",
    "typescript": "~4.7.0",
    "jest": "~28.1.0",
    "ts-jest": "~28.0.0",
    "@types/jest": "~27.5.0",
    "@jest/types": "~28.1.0"
  }
}