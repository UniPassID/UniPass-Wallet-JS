{
  "name": "@unipasswallet/relayer",
  "version": "0.0.1-alpha.6",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@unipasswallet/abi": "workspace:^0.0.1-alpha.10",
    "@unipasswallet/keys": "workspace:^0.0.1-alpha.10",
    "@unipasswallet/network": "workspace:^0.0.1-alpha.10",
    "@unipasswallet/transactions": "workspace:^0.0.1-alpha.10",
    "@unipasswallet/utils": "workspace:^0.0.1-alpha.10",
    "ethers": "^5.7.1",
    "fetch-ponyfill": "^7.1.0"
  },
  "devDependencies": {
    "@types/node": "^18.7.18",
    "rimraf": "~3.0.2",
    "typescript": "~4.8.3"
  }
}
