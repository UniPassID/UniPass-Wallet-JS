{
  "name": "@unipasswallet/transactions",
  "version": "0.0.1-alpha.2",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@unipasswallet/abi": "workspace:^0.0.1-alpha.2",
    "@unipasswallet/keys": "workspace:^0.0.1-alpha.2",
    "@unipasswallet/utils": "workspace:^0.0.1-alpha.2",
    "@unipasswallet/wallet": "workspace:^0.0.1-alpha.3",
    "ethers": "^5.7.0"
  },
  "devDependencies": {
    "@types/node": "^18.7.16",
    "rimraf": "~3.0.2",
    "typescript": "~4.7.4"
  }
}
