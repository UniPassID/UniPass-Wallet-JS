{
  "name": "@unipasswallet/wallet",
  "version": "0.0.1-alpha.4",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "test": "jest --testTimeout 30000",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@unipasswallet/keys": "workspace:^0.0.1-alpha.3",
    "@unipasswallet/utils": "workspace:^0.0.1-alpha.3",
    "ethers": "^5.7.0"
  },
  "devDependencies": {
    "@jest/types": "~28.1.3",
    "@types/jest": "~27.5.2",
    "@types/node": "^18.7.16",
    "jest": "~28.1.3",
    "rimraf": "~3.0.2",
    "ts-jest": "~28.0.8",
    "typescript": "~4.7.4"
  }
}
